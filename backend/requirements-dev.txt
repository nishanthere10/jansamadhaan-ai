# Dev/test dependencies — install with:
#   pip install -r requirements-dev.txt
# (or: uv pip install -r requirements-dev.txt)
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27          # used by FastAPI TestClient
anyio>=4.0
ruff>=0.16           # lint (config: ruff.toml)
vulture>=2.14        # dead-code scan; run as:
                     #   vulture app vulture_whitelist.py --min-confidence 60
